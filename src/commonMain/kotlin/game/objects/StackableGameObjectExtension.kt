package game.objects

open class StackableGameObjectExtension(gameObject: GameObject) : GameObjectClientExtension(gameObject) {

    override fun update(delta: Double) {
        super.update(delta)
    }
}